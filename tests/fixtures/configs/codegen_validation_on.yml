strategy:
  name: codegen_validation_on_fixture
  type: codegen
  path: __RUN_PATH__
  params:
    train:
      model: gpt-5-mini
      reasoning: low
      rounds: 0
      refresh_every: 1
      search_tools:
        - bm25
      edit:
        guards:
          - validation
      eval:
        train_fraction: 0.5
        seed: 123
        eval_margin: 0.0
      system_prompt: |
        Improve the reranker.
    run:
      top_k: 5
