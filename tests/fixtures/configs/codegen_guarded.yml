strategy:
  name: codegen_guarded_fixture
  type: codegen
  path: tests/fixtures/past_runs/20260502_025238
  params:
    train:
      model: gpt-5-mini
      reasoning: low
      refresh_every: 1
      search_tools:
        - fielded_bm25
        - minilm
      edit:
        guards:
          - validation
          - length:
              max_lines: 5
              max_cols: 120
      eval:
        train_fraction: 0.2
        seed: 123
        eval_margin: 0.0
      system_prompt: |
        Improve the reranker.

    run:
      top_k: 5
