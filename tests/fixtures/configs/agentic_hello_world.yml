strategy:
  name: agentic_hello_world_fixture
  type: agentic
  params:
    model: gpt-5-mini
    reasoning: low
    system_prompt: |
      You take user search queries and use a search tool to find products.
    search_tools:
      - e5_base_v2
